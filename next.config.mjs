/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.pixabay.com",
      "www.svgrepo.com",
      "i.pravatar.cc",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
